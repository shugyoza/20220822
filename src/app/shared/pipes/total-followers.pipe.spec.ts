import { TotalFollowersPipe } from './total-followers.pipe';

describe('TotalFollowersPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalFollowersPipe();
    expect(pipe).toBeTruthy();
  });
});
